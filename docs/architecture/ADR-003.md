---
id: ADR-003
kind: adr
title: Quality Gates
status: accepted
date: 2026-05-17T00:00:00.000Z
authors: [Jonathan Gadea Harder]
reviewers: [Jonathan Gadea Harder]
tags: []
supersedes: []
superseded_by: []
depends_on: []
blocks: []
implements: []
related: []
external: []
project: mac-control-mcp
checksum: 8bdb96b2848a780169d0605e32cc1f2db097a3c6b878bbefcf6730412fb7951f
---

## Context

The project needs CI/CD quality gates to enforce code quality, security scanning, and test coverage on both PR and merge workflows. Since this is a macOS-specific project, tests are split into three categories with different run requirements.

## Decision

Three GitHub Actions workflows plus OpenCodeReview:

### pr-gate.yml
Runs on every PR (self-hosted macOS arm64 runner). Includes:
- `ruff format --check` (format)
- `ruff check` (lint)
- `pyright` (type check)
- `uv build` (build)
- `pytest --cov --cov-branch --cov-fail-under=90` (unit tests)
- `mutmut run --paths-to-mutate <changed>` (mutation tests on changed files)
- `vulture` (dead code detection)
- `trivy` filesystem scan (dependency audit)
- `semgrep` (security scanning)

### merge-gate.yml
Runs on push to main (self-hosted macOS arm64 runner). Includes everything in pr-gate plus:
- Full `mutmut run` (mutation testing, not just changed files)
- SonarQube (local) full analysis via `sonar-scanner` against `http://127.0.0.1:9001`
- E2E tests (macOS runner only, if available)

### OpenCodeReview (OCR)
- `code-review.yml` / `ocr-review.yml` post review comments and gate on findings
- Runs via `/usr/local/bin/ocr` on the self-hosted macOS ARM64 runner
- OCR is the sole automated reviewer.

### SonarQube (local)
- Runs on the self-hosted macOS runner via `sonar-scanner`
- Server: local Docker `sonarqube` container, `http://127.0.0.1:9001`
- Token: repo secret `SONAR_TOKEN` (no SonarCloud — retired)
- `sonar-project.properties` pins host + coverage report path

### Branch Protection
- `main` branch requires `Required Checks (PR)` status check
- Requires 1 approving review (OpenCodeReview)
- Dismisses stale reviews

## Consequences

- Unit tests run on every PR (self-hosted macOS runner)
- Integration/E2E only tested on macOS
- OpenCodeReview provides automated review
- Local SonarQube maintains quality history and enforces gates
- trivy + semgrep catch dependency vulnerabilities and security issues early
